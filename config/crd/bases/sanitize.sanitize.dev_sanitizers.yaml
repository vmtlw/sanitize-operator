---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: sanitizers.sanitize.sanitize.dev
spec:
  group: sanitize.sanitize.dev
  names:
    kind: Sanitizer
    listKind: SanitizerList
    plural: sanitizers
    singular: sanitizer
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Sanitizer is the Schema for the sanitizers API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SanitizerSpec defines the desired state of Sanitizer.
            properties:
              schedule:
                description: Schedule defines the cron schedule for cleanup execution.
                type: string
            type: object
          status:
            description: SanitizerStatus defines the observed state of Sanitizer.
            properties:
              lastCleanupTime:
                description: LastCleanupTime records the last time cleanup was performed.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
